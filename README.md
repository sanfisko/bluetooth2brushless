# 🚗 bluetooth2brushless

ESP32 + пульт BT13 = управление бесщеточным двигателем

## 🚀 Быстрый старт

### Автоматическая установка (рекомендуется)
```bash
# Клонирование репозитория
git clone https://github.com/sanfisko/bluetooth2brushless.git
cd bluetooth2brushless

# Запуск автоматической установки
./install.sh

# Быстрая установка (пропуск проверки обновлений)
./install.sh --skip-updates
```

Скрипт автоматически:
- ✅ Проверит системные зависимости (git, python3, curl, pip)
- ✅ Установит ESP-IDF если не найден
- ✅ Проверит и предложит обновления ESP-IDF
- ✅ Активирует ESP-IDF окружение
- ✅ Соберет проект
- ✅ Найдет ESP32
- ✅ Прошьет ESP32 (скорость 115200 для надежности)
- ✅ Запустит мониторинг (выход: **Ctrl+]**)

### Параметры установки

**Обычная установка:**
```bash
./install.sh
```
- Полная проверка системы и ESP-IDF
- Предложение обновления ESP-IDF
- Интерактивные вопросы

**Быстрая установка:**
```bash
./install.sh --skip-updates
```
- Пропуск проверки обновлений ESP-IDF
- Ускоренный процесс установки
- Подходит для повторных запусков

### Системные требования

Скрипт автоматически проверит и подскажет как установить:
- **git** - для загрузки ESP-IDF
- **python3** - основа ESP-IDF  
- **curl** - для загрузки зависимостей
- **pip3** - менеджер пакетов Python

Команды установки для разных систем:
```bash
# Ubuntu/Debian
sudo apt update && sudo apt install git python3 curl python3-pip

# CentOS/RHEL/Fedora  
sudo yum install git python3 curl python3-pip

# macOS
brew install git python3 curl
```

### Ручная установка

#### 1. Требования
- **ESP-IDF v5.4+** (обязательно)
- ESP32 DevKit
- USB кабель
- Пульт BT13

#### 2. Установка ESP-IDF
```bash
mkdir -p ~/esp && cd ~/esp
git clone --recursive https://github.com/espressif/esp-idf.git
cd esp-idf && ./install.sh esp32 && . ./export.sh
```

#### 3. Сборка и прошивка
```bash
# Активация ESP-IDF (в каждой новой сессии)
. ~/esp/esp-idf/export.sh

# Установка цели ESP32
idf.py set-target esp32

# Сборка проекта
idf.py build

# Прошивка (замените /dev/ttyUSB0 на ваш порт)
idf.py -p /dev/ttyUSB0 flash

# Прошивка с пониженной скоростью (для проблемных кабелей)
idf.py -p /dev/ttyUSB0 -b 115200 flash

# Мониторинг (Ctrl+] для выхода)
idf.py -p /dev/ttyUSB0 monitor
```

## 🔌 Подключение

### Схема подключения
```
ESP32 GPIO 25 → ESC PWM вход (сигнал скорости)
ESP32 GPIO 26 → ESC реверс (направление)
ESP32 GND     → ESC GND
ESP32 GPIO 2  → LED (индикация состояния)
```

### ⚠️ Важно!
- **Обязательно используйте ESC (Electronic Speed Controller)** для управления бесщеточным двигателем
- **НЕ подключайте ESP32 напрямую к двигателю!** Это может повредить ESP32
- Убедитесь, что ESC поддерживает реверс (двунаправленное вращение)

### Рекомендуемые ESC
- Hobbywing QuicRun 1060 Brushed ESC
- Traxxas XL-5 ESC
- Любой ESC с поддержкой PWM сигнала и реверса

## 🎮 Управление BT13

| Кнопка BT13 | Действие | Описание |
|-------------|----------|----------|
| **+ короткое** | +1 уровень скорости | Увеличивает скорость на 20% |
| **- короткое** | -1 уровень скорости | Уменьшает скорость на 20% |
| **+ длинное** | Максимум вперед | Мгновенно 100% вперед |
| **- длинное** | Максимум назад | Мгновенно 100% назад |
| **Средняя** | СТОП | Полная остановка |

### Логика управления
- **5 уровней скорости**: от -5 (максимум назад) до +5 (максимум вперед)
- **0 уровень**: полная остановка
- **Плавное управление**: короткие нажатия для точной настройки (шаг 20%)
- **Быстрое управление**: длинные нажатия для максимальной скорости
- **Автостоп**: при отключении BT13 двигатель останавливается через 10 секунд

### Мониторинг работы
После прошивки автоматически запускается мониторинг ESP32:
- Отображает логи подключения к BT13
- Показывает команды от пульта в реальном времени
- Выводит текущий уровень скорости и направление
- Индикация состояния PWM сигналов

**Команды мониторинга:**
- **Ctrl+]** - выход из мониторинга
- После выхода - автоматический анализ подключения
- При успешном подключении - предложение удаления ESP-IDF

**Пример вывода мониторинга:**
```
I (12345) BT_HID: HID Usage: 0x00B5 (+ короткое нажатие)
I (12346) MOTOR: Command: Short +, Speed level = 1
I (12347) MOTOR: PWM: 51/255, Direction: FORWARD
I (12348) LED: State: ON
```

## 🔧 Настройка

### Изменение пинов
Отредактируйте `main/main.c`:
```c
#define MOTOR_SPEED_PIN     GPIO_NUM_25  // PWM сигнал
#define MOTOR_DIR_PIN       GPIO_NUM_26  // Направление
#define LED_PIN             GPIO_NUM_2   // Индикация
```

### Изменение MAC адреса BT13

⚠️ **ВАЖНО**: Поиск пульта происходит **ТОЛЬКО по MAC адресу**, а не по имени устройства!

#### 🔍 Как найти MAC адрес вашего BT13

**Способ 1: Через телефон/компьютер**
1. Включите BT13 (долгое нажатие средней кнопки, синий LED мигает)
2. На телефоне/ПК откройте настройки Bluetooth
3. Найдите устройство "BT13" в списке
4. Посмотрите MAC адрес (формат: `XX:XX:XX:XX:XX:XX`)

**Способ 2: Через логи ESP32**
1. Запустите проект с любым MAC адресом
2. Включите BT13
3. В логах найдите строку: `Found device: xx:xx:xx:xx:xx:xx`
4. Это и есть MAC адрес вашего BT13

#### 🔧 Изменение MAC адреса в коде

Откройте файл `main/main.c` и найдите **строку 44**:
```c
static esp_bd_addr_t bt13_addr = {0x8B, 0xEB, 0x75, 0x4E, 0x65, 0x97};
```

Замените MAC адрес на ваш. **Пример**:
- Ваш MAC: `AA:BB:CC:DD:EE:FF`
- Измените на: `{0xAA, 0xBB, 0xCC, 0xDD, 0xEE, 0xFF}`

```c
// Было (по умолчанию):
static esp_bd_addr_t bt13_addr = {0x8B, 0xEB, 0x75, 0x4E, 0x65, 0x97};

// Стало (ваш MAC):
static esp_bd_addr_t bt13_addr = {0xAA, 0xBB, 0xCC, 0xDD, 0xEE, 0xFF};
```

#### 📝 Правила конвертации MAC адреса
- MAC `8B:EB:75:4E:65:97` → `{0x8B, 0xEB, 0x75, 0x4E, 0x65, 0x97}`
- Каждая пара символов → `0xXX`
- Разделители `:` убираются
- Добавляется префикс `0x` к каждой паре

#### ✅ После изменения
1. Сохраните файл
2. Пересоберите проект: `idf.py build`
3. Прошейте ESP32: `idf.py flash`
4. Включите BT13 и проверьте подключение

## 📊 Ожидаемые логи

При успешном подключении вы увидите:
```
=== ESP32 HID Host Motor Control ===
System initialization...
Motor initialized
Bluetooth initialized
Searching for BT13 remote (MAC: 8B:EB:75:4E:65:97)...
Found device: 8b:eb:75:4e:65:97
Found BT13! Stopping discovery...
Connecting to BT13...
BT13 connected successfully!
Ready to receive commands from remote
```

При нажатии кнопок:
```
HID data (3 bytes): 01 B5 00
HID Usage: 0x00B5
Command: Short + (level increase)
Short +: Speed level = 1 (10% forward)
State: ON | Level: 1/10 | PWM: 25/255 | Direction: FORWARD
```

## 🔍 Диагностика

### Проблемы с прошивкой

**ESP32 не найден**
```bash
# Проверьте доступные порты
ls /dev/tty* | grep -E "(USB|ACM)"

# Попробуйте разные порты
idf.py -p /dev/ttyUSB1 flash
idf.py -p /dev/ttyACM0 flash
```

**Ошибки прошивки / плохой кабель**
```bash
# Используйте пониженную скорость
idf.py -p /dev/ttyUSB0 -b 115200 flash

# Или очень медленную скорость
idf.py -p /dev/ttyUSB0 -b 9600 flash

# Прямая прошивка через esptool
python -m esptool --chip esp32 -p /dev/ttyUSB0 -b 115200 \
  --before default_reset --after hard_reset write_flash \
  --flash_mode dio --flash_freq 40m --flash_size 2MB \
  0x1000 build/bootloader/bootloader.bin \
  0x10000 build/bt13_motor_control.bin \
  0x8000 build/partition_table/partition-table.bin
```

**Автоматический скрипт установки**
- Автоматическая установка ESP-IDF если не найден
- Проверка и предложение обновлений ESP-IDF
- Проверка системных зависимостей
- Скорость прошивки 115200 для надежности с любыми кабелями
- При ошибке автоматически пробует альтернативные методы прошивки

**Новые возможности скрипта:**
- Полностью автономная установка - от нуля до готового устройства
- Умная проверка окружения и зависимостей
- Автоматическое обновление ESP-IDF до последней версии
- Анализ логов подключения к BT13
- Умное удаление ESP-IDF после успешной работы (освобождение ~2-3 ГБ)
- Совместимость с Ubuntu, CentOS, Fedora, macOS
- Умное обновление ESP-IDF с автоматическим решением конфликтов
- Резервное копирование при проблемах обновления

**Управление дисковым пространством:**
- Автоматический анализ подключения к BT13 по логам
- Предложение удаления ESP-IDF после успешной работы
- Опция архивирования вместо удаления
- Возможность принудительного удаления
- Автоматическая переустановка ESP-IDF при повторном запуске

### Решение проблем с обновлением ESP-IDF

**Проблема: "divergent branches" при обновлении**
```
fatal: Need to specify how to reconcile divergent branches.
```

**Решение:** Скрипт автоматически:
- Сохраняет локальные изменения в stash
- Настраивает стратегию merge для git pull
- Выполняет сброс к последней версии origin/master
- Предлагает полную переустановку при неудаче

**Ручное решение:**
```bash
cd ~/esp/esp-idf
git stash
git config pull.rebase false
git reset --hard origin/master
git pull origin master
git submodule update --init --recursive
```

### Анализ подключения к BT13

Скрипт автоматически анализирует логи ESP32 для определения успешного подключения:

**Признаки успешного подключения:**
- Сообщения о подключении BLE/HID устройств
- Активность HID команд (нажатия кнопок пульта)
- Обнаружение MAC адреса BT13 в логах
- Изменения уровня скорости и PWM

**Паттерны поиска:**
```
HID Usage: 0x00B5          # Кнопка + короткое нажатие
Command: Short +           # Команда увеличения скорости  
Speed level = 1            # Изменение уровня скорости
PWM: 51/255               # Установка PWM сигнала
Direction: FORWARD        # Направление движения
```

**После успешного подключения:**
- Предложение удаления ESP-IDF для освобождения места
- Опции: удалить, архивировать или оставить
- Сохранение возможности повторной установки

**Выход из мониторинга:**
- Нажмите **Ctrl+]** для выхода из режима мониторинга
- После выхода скрипт автоматически проанализирует логи
- При обнаружении подключения к BT13 предложит удаление ESP-IDF

### BT13 не подключается

⚠️ **ЧАСТАЯ ОШИБКА**: Неправильный MAC адрес в коде!

**Симптомы**:
- В логах видно: `Found device: XX:XX:XX:XX:XX:XX`
- Но нет строки: `Found BT13! Stopping discovery...`
- ESP32 продолжает поиск бесконечно

**Решение**:
1. **Проверьте MAC адрес** в коде (строка 44 в `main/main.c`)
2. **Сравните** с MAC в логах `Found device:`
3. **Исправьте MAC** если они не совпадают
4. **Пересоберите и прошейте** проект

**Другие причины**:
1. **Зарядите BT13** (красный LED должен погаснуть)
2. **Включите BT13**: долгое нажатие средней кнопки (синий LED мигает)
3. **Перезагрузите ESP32** если нужно
4. **Проверьте расстояние** (должно быть < 10 метров)

### Двигатель не вращается
1. **Проверьте подключение ESC**
2. **Убедитесь что ESC включен и настроен**
3. **Проверьте питание ESC и двигателя**
4. **Проверьте PWM сигнал** (должен быть 0-255)

### Ошибки сборки
```bash
# Очистка и пересборка
idf.py clean
idf.py build

# Проверка версии ESP-IDF
idf.py --version
```

## 📁 Структура проекта

```
bluetooth2brushless/
├── README.md              # Этот файл
├── LICENSE                # Лицензия MIT
├── CMakeLists.txt         # Конфигурация проекта
├── sdkconfig.defaults     # Настройки ESP-IDF
├── install.sh             # Скрипт автоустановки
└── main/
    ├── CMakeLists.txt     # Конфигурация компонента
    └── main.c             # Основной код
```

## 🛡️ Безопасность

- **Всегда используйте ESC** между ESP32 и двигателем
- **Проверьте полярность** подключения
- **Не превышайте номинальные токи** ESP32 (максимум 40mA на пин)
- **Используйте предохранители** в силовых цепях
- **Тестируйте на малых скоростях** перед полной нагрузкой

## 📝 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи ESP32 через `idf.py monitor`
2. Убедитесь в правильности подключения
3. Проверьте версию ESP-IDF (требуется v5.4+)
4. Создайте issue в репозитории с подробным описанием проблемы

---

**MAC BT13 по умолчанию**: `8B:EB:75:4E:65:97`